import React from 'react';
import { Box } from '@material-ui/core';
import styles from './styles';

export const UserEntireSearchPage: React.FC = () => {
  const classes = styles();
  return <Box className={classes.navbar}>LoadingComponent</Box>;
};

export default UserEntireSearchPage;
